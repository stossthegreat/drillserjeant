// Project-level Gradle file. Keep it minimal; let Flutter manage plugin versions.
buildscript {
    repositories {
        google()
        mavenCentral()
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
